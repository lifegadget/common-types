{
  "name": "common-types",
  "version": "1.10.11",
  "description": "Common types not included in Typescript",
  "repository": "https://github.com/lifegadget/common-types",
  "main": "dist/common-types.cjs.js",
  "module": "dist/common-types.es2015.js",
  "browser": "dist/common-types.umd.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rollup -c && yarn publish",
    "build": "rollup -c",
    "build:tsc": "tsc && tsc -P tsconfig-esm.json"
  },
  "author": "Ken Snyder <ken@ken.net>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "rollup": "^1.9.0",
    "rollup-plugin-typescript2": "^0.20.1",
    "typescript": "^3.4.2"
  }
}
